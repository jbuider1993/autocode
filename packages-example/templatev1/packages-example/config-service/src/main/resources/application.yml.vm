info:
  component: Config Server
server:
  port: 8888  
spring:
  application:
    name: configserver  
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  profiles:
     active: native